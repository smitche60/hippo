# The rules are in AGENTS.md

This brain's operating rules live in `AGENTS.md`, alongside `RESOLVER.md`, `ONTOLOGY.md`,
`REDACTOR.md`, and `CONTEXT.md`. Read `AGENTS.md` first and follow it; this file exists only
so that assistants which load `CLAUDE.md` automatically find their way there.
